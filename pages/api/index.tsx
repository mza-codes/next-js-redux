import Head from "next/head";

export default function index() {
    return (
        <main>
            <Head>
                <title>Home | mFlux</title>
            </Head>
        </main>
    );
}
